version: '3'
services:
  db:
    image: postgres:latest
    restart: always
    environment:
      LANG: en_US.UTF-8
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: geonames
    ports:
      - 5432:5432
    volumes:
      - ./import:/docker-entrypoint-initdb.d
      - ./export:/export
